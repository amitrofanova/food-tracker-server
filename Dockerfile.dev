FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3001

# Generate Prisma client then start ts-node dev server
CMD ["sh", "-c", "npx prisma generate && npx prisma migrate deploy && npm run dev"]
